# SKIP_LINTERS disables every linter but still builds nothing, runs tests, and
# enforces the coverage threshold. Submodule tests are skipped.
SKIP_LINTERS=yes
COVER_THRESHOLD=85.0
SKIP_SUBMODULE_TESTS=yes
STUB_SUBMODULE=1
