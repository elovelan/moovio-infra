# Retracted modules: one unused (info), one in the skip list (info), and one
# that is used (error, exit 1).
STUB_RETRACTED=github.com/unused/x github.com/moby/sys/user github.com/foo/bar
STUB_UNUSED_RETRACTED=github.com/unused/x
